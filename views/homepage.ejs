<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Homepage</title>
</head>
<body>
    <h1>Messages</h1>
    <ul>
        <% messages.forEach(msg => { %>
          <li><strong><%= msg.first_name %></strong>: <%= msg.message %> (at <%= msg.created_at.toLocaleString() %>)</li>
        <% }) %>
      </ul>
    
</body>
</html>